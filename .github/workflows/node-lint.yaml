name: Node Lint

on:
  workflow_call:
    inputs:
      runs-on:
        description: >-
          Runner label. Defaults to ubuntu-latest. Override to a self-hosted
          runner label if you want to run CI on your own scale set.
        required: false
        default: ubuntu-latest
        type: string
      node-version:
        description: Node.js version to install
        required: false
        default: "22"
        type: string
      working-directory:
        description: Directory containing package.json
        required: false
        default: "."
        type: string
      lint-script:
        description: npm script name for the linter
        required: false
        default: lint
        type: string
      tsc-script:
        description: npm script name for TypeScript type checking
        required: false
        default: tsc
        type: string

permissions:
  contents: read

jobs:
  eslint:
    name: eslint
    runs-on: ${{ inputs.runs-on }}
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
          cache-dependency-path: ${{ inputs.working-directory }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run eslint
        id: eslint
        run: npm run ${{ inputs.lint-script }}

      - name: eslint summary
        if: always()
        run: |
          if [ "${{ steps.eslint.outcome }}" = "success" ]; then
            {
              echo "### eslint"
              echo "Node linting passed."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            {
              echo "### eslint"
              echo "Node linting failed."
              echo ""
              echo "- Review the job logs for full details."
            } >> "$GITHUB_STEP_SUMMARY"
          fi

  tsc:
    name: tsc
    runs-on: ${{ inputs.runs-on }}
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
          cache-dependency-path: ${{ inputs.working-directory }}/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tsc
        id: tsc
        run: npm run ${{ inputs.tsc-script }}

      - name: tsc summary
        if: always()
        run: |
          if [ "${{ steps.tsc.outcome }}" = "success" ]; then
            {
              echo "### tsc"
              echo "TypeScript type checking passed."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            {
              echo "### tsc"
              echo "TypeScript type checking failed."
              echo ""
              echo "- Review the job logs for full details."
            } >> "$GITHUB_STEP_SUMMARY"
          fi
