name: PR

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/**"
      - ".github/semantic-release/**"
      - "README.md"

permissions:
  contents: read

jobs:
  validate:
    name: Validate
    uses: ./.github/workflows/validate.yaml
